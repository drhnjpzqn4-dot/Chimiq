{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:mobile": "pnpm --filter @workspace/skinscreen run build && cd artifacts/skinscreen/mobile/capacitor && (test -d node_modules || npm install --silent --no-audit --no-fund) && npx cap sync"
  },
  "private": true,
  "devDependencies": {
    "@esbuild/darwin-arm64": "0.27.3",
    "@rollup/rollup-darwin-arm64": "^4.60.3",
    "@tailwindcss/oxide-darwin-arm64": "^4.2.1",
    "lightningcss-darwin-arm64": "1.31.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "stripe": "^22.0.0"
  }
}
