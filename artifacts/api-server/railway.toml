[build]
builder = "NIXPACKS"

[deploy]
# NODE_ENV sätts explicit: sessionskakan använder Secure i produktion (krav för https://app.chimiq.app m.fl.).
startCommand = "NODE_ENV=production node --enable-source-maps ./dist/index.mjs"

